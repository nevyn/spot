package se.despotify;

/**
 * Constants for browsing media.
 */
public enum BrowseType {
  artist(1), album(2), track(3);

  private int value;

  BrowseType(int value) {
    this.value = value;
  }

  public int getValue() {
    return value;
  }
}
